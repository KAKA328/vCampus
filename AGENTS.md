# 项目开发协作规范

## Git 与 Pull Request

- `main` 是受保护的可构建主线，禁止直接提交、强制推送或绕过 Pull Request 合并。
- 每项功能或修复从最新 `origin/main` 创建独立分支；完成后提交中文 PR，说明改动范围、影响模块、验证方式和兼容性。
- PR 合并前必须通过 CI、至少一名成员审查，并确认没有未解决的冲突。公共分支不做 rebase 或历史重写；需要撤销已发布改动时使用 revert。
- 不覆盖、删除或回退其他成员尚未提交的工作；处理冲突前先确认双方改动的意图和接口影响。

## 公共契约与跨模块对接

- `common` 中的 `MessageType`、DTO、枚举、权限编码和服务接口属于公共契约。修改时必须同步检查客户端调用、服务端 Handler、`ServerApplication` 分发、权限校验、测试和接口文档。
- 协议语义发生变化时新增显式版本化的消息类型或命令类；不得在旧类中偷偷改字段含义，也不得在不兼容的情况下复用原 `serialVersionUID`。
- 涉及学生、教师、用户身份的请求优先携带会话 token，由服务端从会话推导真实 `userId` 及绑定档案编号；不能把客户端传入的身份编号作为权限依据。
- 各子系统保持自己的实体、Service、Repository、客户端页面和测试边界。跨模块字段、消息或数据库约束变更必须在 PR 中写清责任方、调用方和兼容策略。

## 实现与验证

- 输入校验、权限检查和业务规则放在服务端；客户端只负责交互和显示，不能依赖隐藏按钮实现安全控制。
- Access 持久化、事务、库存、选课等跨表操作要保持业务原子性；内存实现只能作为联调替身，不能改变公共接口语义。
- 提交前运行与改动范围匹配的 Maven 测试，并在 PR 中记录实际执行的命令和结果；接口或协议变更必须补充正常、异常和权限场景测试。
- 仅提交源代码、必要文档和测试数据，不提交真实密码、个人敏感信息、IDE 配置或构建产物。
